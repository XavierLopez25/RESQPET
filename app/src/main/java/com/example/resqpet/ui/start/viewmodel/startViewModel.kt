package com.example.resqpet.ui.start.viewmodel

import androidx.lifecycle.ViewModel

class ResqpetViewModel : ViewModel() {

    // Variables para manejar la lógica de negocio y datos

    fun onRegisterClicked() {
        //  Lógica de lo que sucede cuando se hace clic en 'Register'
    }

    fun onLoginClicked() {
        // Lógica de lo que sucede cuando se hace clic en 'Login'
    }
}